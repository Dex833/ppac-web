{
  "name": "ppac-web-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "deploy": "firebase deploy --only functions -P ppac-web",
    "serve": "firebase emulators:start --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0",
    "ppac-web": "file:.."
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.3"
  }
}
